# Frontend application and screen coverage matrix

This matrix covers every application in `NORI_PRODUCTION_APPS`. The status in parentheses is the registry descriptor, not a parity verdict. “Source gap” records a concrete capability still absent from the maintained implementation. “Acceptance pending” records implemented behavior that still needs CI, reference-client, private-agent or broader corpus verification. A blank source gap does not mark a cutover boundary complete.

| App                               | Registered windows and source owner                                                                        | Implemented evidence                                                                                                                                                                                                                                                              | Source gap                                                                                                                      | Acceptance pending                                                                                                      |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `system` (`runtime-recovered`)    | `about`, `alert`; `screens/system-screen.tsx`, `components/about-logo.tsx`                                 | About roll, pointer/reduced-motion logo and localized alert close/title behavior; `frontend:app:smoke`                                                                                                                                                                            | No registered-screen source gap identified.                                                                                     | Full alert payload matrix, reference visuals and final shell/audio timing.                                              |
| `credits` (`metadata-recovered`)  | `main`; `screens/credits-screen.tsx`, `components/credits-brand-icons.tsx`                                 | Shipped capsule, source brand SVGs, reveal/motes, external destinations, clipboard feedback and opened fact; `frontend:app:smoke`                                                                                                                                                 | No registered-screen source gap identified.                                                                                     | Reference typography/timing, both locales, external navigation and clipboard-denial browser acceptance.                 |
| `idle` (`metadata-recovered`)     | `main`; `apps/idle-presentation.tsx`, `screens/idle-*`                                                     | Economy, faction/Heritage effects, assistants, periodic effects, persistence, initialization and QFR Dock are source-owned; runtime/game tests and application smoke                                                                                                              | No registered-screen source gap identified; synthesized initialization cue comparison remains an owning audio edge.             | Long-running progression/persistence, all reset paths, compact layouts and reference visual/audio comparison.           |
| `mail` (`ui-recovered`)           | `main`; `apps/mail-presentation.tsx`, `screens/mail-screen.tsx`                                            | Three-pane folders, read/archive, attachments/download facts, compose failure and focus intents; `tests/test_virtual_apps.py`, application smoke                                                                                                                                  | No registered-screen source gap identified.                                                                                     | Attachment failure/retry, permission failures, resize/focus lifecycle and full authored-mail corpus.                    |
| `files` (`ui-recovered`)          | `main`; `apps/files-presentation.tsx`, `screens/files-screen.tsx`, `apps/files-tree.ts`                    | Tree/history, grid/list, keyboard navigation, vault and locked-file recovery, cold volume/QFR, Preview launch and intents; virtual-app tests and Preview/chip probe                                                                                                               | No registered-screen source gap identified.                                                                                     | Large/deep trees, every vault/recovery route, download failures and reference responsive comparison.                    |
| `browser` (`ui-recovered`)        | `main`, `popup`; Browser presentation, screen, page view/runtime and popup screen                          | Tabs/history/bookmarks, artifact/srcdoc bridge, facts/window/podcast messaging, asset/font inlining and popup handoff; stable window title/status callbacks, void effect cleanups, source-owned bounty-extension permission dialog and `bounty.ext_installed` fact handoff; virtual-app tests and application smoke through the real Doodle page                                                                                                                    | No registered-screen source gap identified.     | Full authored envelope corpus, bounty overlay/reference timing, popup focus/stacking, navigation/error states and reference layout.                      |
| `signal` (`ui-recovered`)         | `main`: `login`, `reset`, `tempPassword`, `messenger`; Signal auth screens and `messenger-screen.tsx`      | Auth/recovery plus Messenger search/chrome, GFM/service thread, attachment/media failure, preview focus, IME/draft/floating conversation; shipped thread read/reread windows, latest-message ordering, 2026-08-31 story-calendar grouping, persistent `signal.read`, shared local-read state and Signal Dock unread badge; source-owned shell notification queue and artifact-delta arrival notifications; Daniel reply/media/interrupt choreography; recovery guards, runtime/backend tests and Chromium Messenger CI | No registered-screen source gap identified. | Original-session visual comparison, original-agent sessions, full authored story corpus, notification visual review and original shell arrival timing. |
| `pictionary` (`ui-partial`)       | `game`: `start`, `game`, `results`; Pictionary presentation, screens and Pixi renderer                     | Cover/help/results, normalized drawing transport, Pixi replay/export, hints, statistics and semantic Live2D reactions; game unit/browser suites                                                                                                                                   | Private-agent snapshot inference/dialogue is not available through the local runtime.                                           | Reference animation/typography, live-agent rounds, both locales, compact/reduced-motion and reconnect lifecycle.        |
| `codenames` (`ui-partial`)        | `game`: `start`, `game`, `results`; Codenames presentation, app/screens, art and forest renderers          | Board/footer, tutorial gates/narrative, reveal/flying cards, sudden death, results, source art and semantic reactions; recovery gates and game suites                                                                                                                             | Scenario injection currently covers the recovered Codenames scenarios only; private-agent dialogue remains external.            | Reference start/results motion and dialogue, real-agent clue/guess sessions, both locales and full reconnect lifecycle. |
| `chess` (`ui-partial`)            | `game`: `main`; Chess presentation, screen, board, pieces and tutorial                                     | Legal play/history, request/result sheets, transition feedback and full 22-ply guided opening; game tests, browser suite and application tutorial probe                                                                                                                           | Private-agent speech/opponent acceptance is not represented by the local deterministic opponent.                                | Reference overlays/timing, resize/input-device matrix and original-agent sessions.                                      |
| `cakeduel` (`protocol-recovered`) | `game`: `start`, `game`, `results`; Cake Duel presentation/runtime and screen/component set                | Source-owned protocol, cards, action state, challenge banner/reveal timing, overlays and routes; recovery gates, cartridge tests and game smoke                                                                                                                                   | No registered-screen source gap identified.                                                                                     | Complete browser lifecycle, reference animation/timing and original-agent/media sessions.                               |
| `terminal` (`ui-partial`)         | `main`; `apps/terminal-presentation.tsx`, `screens/terminal-window.tsx`, terminal filesystem/runtime       | Source terminal model, xterm presentation, recovered command set with command-not-found handling, edit/menu bridge and nested-file handling; runtime and virtual-app tests, application smoke                                                                                     | No registered-screen source gap identified; commands outside the shipped NAS/local filesystem contract are a protocol boundary. | Backend/error states, paste/selection/resize, fonts and reference keyboard behavior.                                    |
| `debug` (`metadata-recovered`)    | `main`; `screens/debug-screen.tsx`, `debug-labs.tsx`, scene editor, corruption preview and advanced tuners | Connection/Scene/Audio/Facts, validated scene editor, Corruption, network fault, ledger-backed compute/time/faction, gesture, reaction, Codenames scenarios, Shatter and Datasea tuners; debug/runtime tests and `frontend_debug_probe.mjs`                                       | Dedicated tuners for the other cinematic families and scenario families beyond the recovered Codenames set are not implemented. | Debug browser probe, real Live2D reaction/model behavior, production-takeover fencing and reference diagnostic layout.  |
| `settings` (`metadata-recovered`) | `main`; `screens/settings-screen.tsx`, audio/graphics/system services                                      | Persisted master/track audio, graphics modes and GPU/resolution behavior, online/socket state, serialized latency and acknowledged reset; audio/runtime tests and application smoke                                                                                               | No registered-screen source gap identified.                                                                                     | Device/browser permission and failure matrix, accessibility, both locales and reference responsive visuals.             |
| `preview` (`metadata-recovered`)  | `main`; `screens/preview-screen.tsx`, `pdf-preview.tsx`                                                    | PDF pagination/zoom/thumbnails/text selection, mixed page sizes, literal markers, training-log presentation and artifact refresh; Preview/chip probe and subscription tests                                                                                                       | No registered-screen source gap identified; kinds outside the recovered artifact parser contract are a protocol boundary.       | Broader PDF/document corpus, malformed/encrypted files, unavailable-media paths and reference rendering comparison.     |

Shared registry and shell behavior is owned by `apps/production-catalog.ts`, `state/production-window-apps.ts`, `state/window-app-registry.ts`, `components/window-screen-router.tsx` and the managed-window components. Baseline evidence includes the recovery-gap and game suites, source application/game smoke, ownership checks and cutover/recovery scripts. Production entry, rollback verification, the story/Live2D boundaries and cross-app private-agent/media acceptance remain separate gates.
